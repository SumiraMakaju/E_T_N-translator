{
  "name": "tmt-translator-extension",
  "version": "2.0.0",
  "description": "Google TMT Hackathon 2026 — Smart trilingual browser extension",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "dotenv": "^16.4.5"
  }
}
